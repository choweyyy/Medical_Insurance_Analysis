# Models

The trained model is not included in this repository because it exceeds GitHub's file size limit.

To generate the model:

1. Open `notebooks/Medical_Insurance_Analysis.ipynb`
2. Run all cells until the training section.
3. The trained model will be saved automatically to the `models/` directory.
